import { ClientShell } from '@/components/ClientShell';

export const metadata = {
  title: 'Human Cell — 3D Interactive Viewer | Biology AR',
  description: 'Explore every organelle of the human cell in interactive 3D and augmented reality.',
};

export default function CellPage() {
  return <ClientShell />;
}
